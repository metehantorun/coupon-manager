apiVersion: v1
kind: Service
metadata:
  name: coupon-manager-service
spec:
  selector:
    app: coupon-manager
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
  type: NodePort